.header
  = render 'products/shared/header'

.index-all
  .index__category
    .products-list
      .products-list__name
        検索結果
      %ul.products
        - @products.each do |product|
          = render partial: 'products/shared/product', locals: { product: product }

.footer
  = render 'products/shared/footer'