= link_to "", class: "product-wrap" do
  %li.product
    .product__image
      - if product.images.present?
        = image_tag product.images.first.images, alt: "product-img"
      -else
        = image_tag "material/icon/icon_no_image.png", alt: "product-non-img", width: "200px", height: "200px"
    .detail
      .product__content
        .product__name
          = product.name
        .product__price
          = product.price
          .product__price__tax
            (税抜)
      .product__evaluation
        .product__evaluation__star-img
          %i.fas.fa-star
        .product__evaluation__score
          ０